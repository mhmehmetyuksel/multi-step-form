import MultiStepForm from "@/components/MultiStepForm";
import Image from "next/image";

export default function Home() {
  return (
    <main className="h-screen flex items-center justify-between bg-slate-300">
      <MultiStepForm />
    </main>
  );
}
